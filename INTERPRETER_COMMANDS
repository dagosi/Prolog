listing. 	  -> List the actual load file.
listing(functor). -> List all the information in the knowledge about
		     functor.
[...]             -> Load a file.
['*.pl'] 	  -> Load a file with their path. 

unify_with_occurs_check(..,..). -> Evaluate the unification between expresions.

\  -> The oppsite way to =.